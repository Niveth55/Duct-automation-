# ── Core (no mandatory third-party deps for geometry/standards/BOM) ──────────

# ── DXF / DWG drawing generation ─────────────────────────────────────────────
ezdxf>=1.1.0

# ── BOM Excel export ─────────────────────────────────────────────────────────
openpyxl>=3.1.0

# ── GUI preview (matplotlib embedded in Tkinter) ──────────────────────────────
matplotlib>=3.7.0
numpy>=1.24.0

# ── YAML layout support (optional – used by duct_automation legacy module) ───
PyYAML>=6.0

# ── Windows / AutoCAD COM interface (optional) ───────────────────────────────
# pywin32>=306; sys_platform == "win32"

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=8.0
